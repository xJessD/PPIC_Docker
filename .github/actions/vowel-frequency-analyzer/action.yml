# Action definition

name: 'Vowel Frequency Analyzer'
description: 'Custom action to analyze vowels frequency.'

inputs:
  fine: 
    description: 'The path to the text file to analyze (e.g., date.txt).'
    required: true

runs: 
  using: 'docker'
  image: 'ghcr.io/xJessD/vowel-frequency-analyzer:latest'
  entrypoint: ['/bin/bash', '/github/scripts/entrypoint.sh']
  env:
    FILE: '${{ inputs.file }}'