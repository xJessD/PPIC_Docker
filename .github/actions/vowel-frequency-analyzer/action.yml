# Action definition

name: 'Vowel Frequency Analyzer'
description: 'Custom action to analyze vowels frequency.'

inputs:
  file: 
    description: 'The path to the text file to analyze (e.g., date.txt).'
    required: true

runs: 
  using: 'docker'
  image: 'Dockerfile'
    
  env:
    FILE: '${{ inputs.file }}'